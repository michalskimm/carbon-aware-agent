# Copy to .env and fill in real values. .env is gitignored — never commit it.

# LLM provider (default: openai)
LLM_PROVIDER=openai
OPENAI_API_KEY=sk-...

# carbon-aware-mcp server
CARBON_MCP_URL=https://carbon-aware-mcp.<your-fqdn>.azurecontainerapps.io/mcp
CARBON_MCP_TOKEN=<your-jwt-bearer-token>